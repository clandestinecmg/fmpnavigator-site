# ==========================================================
# FMP Navigator — Hardened .gitignore (Next.js + npm)
# Protect env/secrets, prevent noisy artifacts, keep lockfiles
# ==========================================================

# === Package managers (keep lockfiles committed!) ===
node_modules/
.pnpm-store/
.pnp/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# === Next.js & build artifacts ===
.next/
out/
build/
dist/
.cache/
.next/cache/
*.tsbuildinfo
next-env.d.ts
.vercel/            # do NOT commit Vercel link tokens

# === Turborepo / swc / tailwind caches (if present) ===
.turbo/
.swc/

# === Coverage & testing ===
coverage/
jest-coverage/
*.lcov
cypress/videos/
cypress/screenshots/

# === Logs & debugging ===
*.log
*.pid
*.seed
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
firebase-debug.log

# === Environment & secrets (critical) ===
# Never commit secrets; provide a redacted .env.example instead.
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
.env.test
.env.sentry-build-plugin
*.pem
*.key
*.crt
*.p12
*.cer
# Token/config files that often contain creds
.npmrc
.netrc
*.keystore
*.jks

# direnv / shell env helpers
.envrc
.direnv/

# === Firebase & Google configs (admin keys etc.) ===
.firebaserc
.firebase/
serviceAccountKey.json
google-services.json
GoogleService-Info.plist

# === Sentry / analytics tooling ===
.sentryclirc
.sentry-build-plugin

# === Linters/formatters caches ===
.eslintcache
.stylelintcache
.prettier-cache

# === OS / Editor ===
.DS_Store
.DS_Store?
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json   # optionally share extension recs
*.swp
*.swo

# === Archives & misc ===
*.zip
*.tar.gz
*.tgz
*.rar

# === Local scratch ===
/tmp/
 /data/
 /secrets/
 /private/
 /local/
 /backups/
notes.txt

# === Merge/patch leftovers ===
*.orig
*.rej

# ==========================================================
# Notes:
# - Keep package-lock.json committed for deterministic builds.
# - Restrict public API keys at the provider (HTTP referrer/domain).
# - Store real secrets ONLY in Vercel env vars (Project → Settings → Env).
# ==========================================================.vercel # keep it, if not already present
