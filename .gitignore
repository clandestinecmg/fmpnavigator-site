# ==========================================================
#  FMP Navigator — Hardened .gitignore
#  Prevents leaks of credentials, env files, and secrets
# ==========================================================

# === Core dependencies ===
node_modules/
.pnpm-store/
.pnp/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# === Next.js & build artifacts ===
.next/
out/
build/
dist/
.vercel/
.cache/
*.tsbuildinfo
next-env.d.ts

# === Coverage & testing ===
coverage/
jest-coverage/
*.lcov
cypress/videos/
cypress/screenshots/

# === Logs & debugging ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
firebase-debug.log
*.log
*.pid
*.seed

# === Environment & secrets (critical section) ===
# Absolutely never commit these.
.env
.env.*
.env.local
.env.development
.env.production
.env.test
.env.sentry-build-plugin
*.pem
*.key
*.crt
*.p12
*.cer

# === OS / Editor junk ===
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json  # optional keep if sharing recs
*.swp
*.swo
*.bak
*.tmp
*~

# === Firebase & Google configs ===
.firebaserc
.firebase/
firebase-debug.log
serviceAccountKey.json
google-services.json
GoogleService-Info.plist

# === Sentry / Analytics / Tooling ===
.sentryclirc
.sentry-build-plugin
.env.sentry-build-plugin

# === Misc build caches ===
.eslintcache
.stylelintcache
.prettier-cache
*.lock

# === OS-generated & archives ===
*.zip
*.tar.gz
*.tgz
*.rar

# === Local tools & data ===
/tmp
/data
/secrets
/private
/local
/backups
notes.txt

# ==========================================================
# Notes:
# - .env* covers both .env and .env.local to protect API keys.
# - Firebase admin key (.pem, .json) is blacklisted.
# - .vercel/ ignored so project link tokens aren’t exposed.
# - .vscode settings excluded except for recommended extension hints.
# ==========================================================
.vercel
# Secrets
.env
.env.local
.env*.backup
# Builds
.next
# Misc
node_modules
# Secrets
.env
.env.local
.env*.backup
# Builds
.next
# Misc
node_modules
